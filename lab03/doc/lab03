Лабораторна робота №3. Розробка лінійних програм

Пустовий Iлля Iванович, гр. КІТ-121в

Завдання: Дано трьох-значне число. Обчислити значення другої цифри в числі.

- перелік вхідних даних:
   - x - трьохзначне число (int).
   - y - число, для розрахункі (int).
   - z - число, для розрахункі, а також рiшення (int).
   
- дослідження результатів роботи програми:
   - Всі числа повинні бути цілими, тому тип даних (int).
   - За значення х = 485. Рішенням програми z має бути 8.
   - для підтвердження коректності роботи програми, зупинено відлагодник на строці з "return 0" та введемо команду "print z". Після вводу команди отримали наступне:
	(lldb) p z
	(int) $0 = 8
	
	Результат вірний, а значить програма працює.

Структура проекту лабораторної роботи:
├── lab03
│   ├── dist
│   │   └── main.bin
│   ├── doc
│   ├── Makefile
│   └── src
│       └── main.c

Висновки: при виконанні лабораторної роботи буди набуті практичні навички створення лінійних програм на мові С, зокрема: визначати типи даних, обчислювати математичні вирази, емулювати операцію зведення до ступеню через операцію множення.
