Лабораторна робота №5.   Циклічні конструкції

Пустовий Iлля Iванович, гр. КІТ-121в

Завдання: Визначити, чи є задане ціле число простим.

Основна частина:
- опис роботи основної функції: Просте число - натуральне (ціле позитивне) число, що має рівно два різні натуральні дільники - одиницю і самого себе.

- перелік вхідних даних:
   - x - число, що входить на перевірку (int).
   - i - змінна, яка існує тільки в рамках циклу та використовується у перевірці. (int).
   
- дослідження результатів роботи програми:
   - при значенні x = 13; Рішенням програми x = 13; якщо число було б не просте х = 1;
   - для підтвердження коректності роботи програми, зупинено відлагодник на строці з "return 0" та введемо команду "print x". Після вводу команди отримали наступне:
	(lldb) p x
	(int) $0 = 13
	
Результат вірний, а значить програма працює.

Структура проекту лабораторної роботи:
├── lab05
│   ├── dist
│   │   └── main.bin
│   ├── doc
│   ├── Makefile
│   └── src
│       └── main.c

Висновки: при виконанні лабораторної роботи буди набуті практичні навички створення програм, якi мають циклiчнi конструкцii.
